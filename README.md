# Project Repository

This repository contains two containers:
- react_js_frontend (port 3000): React SPA with Ocean Professional minimalist theme
- backend (port 3001): FastAPI server with mock endpoints and CORS enabled

See each container's README for setup instructions.