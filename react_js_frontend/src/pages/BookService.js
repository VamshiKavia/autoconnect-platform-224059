import BookServicePage from "./bookService/BookService.jsx";

/**
// PUBLIC_INTERFACE
 * BookService - Route component for /book-service that renders the multi-step booking flow.
 */
export default function BookService() {
  return <BookServicePage />;
}
