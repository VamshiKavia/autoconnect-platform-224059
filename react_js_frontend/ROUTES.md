# Routes

- /               Home (Latest Launches)
- /services       Services
- /parts          Spare Parts
- /service-centers Service Centers (with map placeholder)
- /profile        Profile (mock edit)
- /auth           Auth (page exists; not linked in navbar by default)

All API calls are made via src/api/client.js using REACT_APP_API_BASE or REACT_APP_BACKEND_URL.
